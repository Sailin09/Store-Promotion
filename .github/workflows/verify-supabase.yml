name: Verify Supabase Data

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "supabase/migrations/**"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Install PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: Verify Store Promotion database
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -Atc "
            select 'shops=' || count(*) from shops;
            select 'classified=' || count(*) from shops where primary_group is not null;
            select 'products=' || count(*) from products;
            select 'pinterest_variants=' || count(*) from content_variants where platform='pinterest';
            select 'facebook_variants=' || count(*) from content_variants where platform='facebook';
            select 'draft_queue=' || count(*) from publish_queue where status='draft';
            select 'mapped_social_accounts=' || count(*) from shop_social_accounts where active=true;
          "
